# PW Credentials

Sensitive local credential notes for ProjectWorkbench on PVI2.

Rules:
- Copy this template to `/opt/project-workbench/memory/CREDENTIALS.md` during install.
- This file is local-only and must never be committed with real secrets.
- Prefer pointers to existing credential stores where practical.
- Include enough context that Claude/PW can use the credential safely.

